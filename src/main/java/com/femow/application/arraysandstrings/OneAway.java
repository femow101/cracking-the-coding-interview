package com.femow.application.arraysandstrings;

public class OneAway {
    // if the len is diff (sec bigger) -> means that all chjaracters must be the same, but one is added to the second arr
    // if the len is diff (fir bigger) -> means that lal characters mut be the same, but first has one more than sec
    // if the len is same -> check if only one change

    public static boolean checkIsOneAway(String s1, String s2) {
        return false;
    }
}
